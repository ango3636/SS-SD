"""Suturing pipeline data package.

Import submodules explicitly, e.g. ``from suturing_pipeline.data.jigsaws_dataset import JIGSAWSDataset``.
We do not re-export ``alignment`` / ``loader`` here so minimal checkouts (Colab) only need
``jigsaws_dataset`` + ``data_utils`` without optional modules on disk.
"""
